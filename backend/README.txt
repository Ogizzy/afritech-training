This is a sample backend folder for a real MySQL database.
1. Import database.sql into MySQL.
2. Update db.php with your database username/password.
3. Build PHP endpoints for register/contact/admin if you want server database instead of localStorage.

The current Netlify/static version uses browser localStorage, so it works without hosting backend.
